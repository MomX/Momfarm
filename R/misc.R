# Aliases for meaningful code -------
fail <- function(x) crayon::red(x)
warn <- function(x) crayon::yellow(x)
mess <- function(x) crayon::green(x)
# c(mess("Three"), warn("little"), fail("birds")) %>% cat()
